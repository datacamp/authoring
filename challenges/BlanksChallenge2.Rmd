## Functions - 1

```yaml
type: BlanksChallenge 
key: 110S8XaaKc0PS2C8ouCBHLqvA87fPVrumZ6
reference: https://github.com/datacamp/challenges-intermediate-r/blob/master/challenges/chapter3.Rmd#functions---1
```

`@code`

```{r}
# blanks: {from: [fun1, var1, fun2], choose: 2}
positive_or_negative <- {{ fun1 }}({{ var1 }}) {
    if (x > 0) {
        y <- "x is positive"
    } else {
        y <- "x is negative"
    }
    {{ fun2 }}(y)
}
positive_or_negative({{ var2 }})
```

`@variables`

```{yaml}
var1: x
var2: [25, 50, -25, 7.5]
fun1: function
fun2: return
distractors: ['y', 'else if', 'print', 'if', 'require', 'send', 'function', 'return'] 
```
